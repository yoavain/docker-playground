{
  "name": "docker-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Learning Docker",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start",
    "predocker-compose:up": "docker-compose down && npm run build",
    "docker-compose:up": "docker-compose -f docker-compose.yml up -d"
  }
}
