{
  "name": "docker-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Learning Docker",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build --stream",
    "start": "lerna run start --stream",
    "predocker-compose:up": "docker-compose -f docker-compose.json down && npm run build",
    "docker-compose:up": "docker-compose -f docker-compose.json build && docker-compose -f docker-compose.json up -d"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
