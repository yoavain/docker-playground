{
  "name": "@docker-playground/server",
  "version": "1.0.0",
  "private": true,
  "description": "Learning Docker",
  "main": "dist/index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "prebuild": "rimraf dist",
    "cleanup": "docker rm -f server && docker rmi -f server",
    "build": "tsc -p tsconfig.build.json",
    "prestart": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.58.0",
    "dotenv": "16.0.0",
    "express": "4.17.3"
  },
  "devDependencies": {
    "@tsconfig/node14": "1.0.1",
    "@types/node": "17.0.23",
    "rimraf": "3.0.2",
    "typescript": "4.6.3"
  }
}
